from django.conf.urls import url

# from views import *

urlpatterns = [
    # url(r'^browser/$', views.Browser.as_view(), name='device-list'),
    # url(r'^register/$', views.RegisterDevice.as_view(), name='device-register'),
]